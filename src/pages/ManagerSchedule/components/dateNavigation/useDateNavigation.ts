/**
 * Date navigation hook - now handled by useManagerSchedulePersistence
 * This hook is kept for backward compatibility but does nothing
 * Date URL sync is now handled in useManagerSchedulePersistence hook
 */
export function useDateNavigation() {
  // Date initialization and URL sync is now handled in useManagerSchedulePersistence
  // This hook is kept to avoid breaking changes but does nothing
}
